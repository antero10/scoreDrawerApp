version: 2
jobs:
 build:
  branches:
      only:
      - master
   machine: true
   steps:
     - checkout
     - run: |
         docker login -u $DOCKER_USER -p $DOCKER_PASS
     - run: docker build -t brandm3dia/starbucks .